# vim: syntax=python

Import('env', 'arch', 'buildtype')

sources = [
	'colorchart.c',
	'console.c',
	'pong.c',
]

user = env.Clone()
user.Append(LINKFLAGS=['-Map', 'user/user.map'])

if arch == 'x86':
	sources += [
	]

sources += [
]

user.Library('user', sources)
