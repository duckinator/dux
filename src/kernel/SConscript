Import('env', 'arch')

sources = Split('''
	init/init.c
''')

o = SConscript('arch/%s/SConscript' % arch)
sources.append(o)

env.Append(LINKFLAGS=['-T', 'src/kernel/link.ld'])
env.Append(CCFLAGS=['-I', 'include/arch/%s' % arch])

env.Program('kernel', sources)
