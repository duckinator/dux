# vim: syntax=python

Import('env', 'arch', 'buildtype', 'distreq')

hal = env.Clone()

sources = [
	'gdt.asm',
	'hal.c',
	'idt.c',
	'ports.c',
	'display.c',
]

distreq += ['src/metodo/hal/i386/hal.lib']

hal.StaticLibrary('hal', sources)

